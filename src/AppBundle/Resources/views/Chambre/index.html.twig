{% extends 'base.html.twig' %}

{% block body %}
    {#{% if is_granted("IS_AUTHENTICATED_FULLY") %}#}
    {#<div class="alert alert-primary notif-username" role="alert">
        <div class="container">
            <div class="row">
                <div class="col-12 text-right">
                    <button class="btn close" onclick="closeNotif()"><i class="fa fa-times-circle-o" aria-hidden="true"></i></button>
                </div>
                <div class="col-12 text-center">
                    <p>Bonjour {{ username }}</p>
                    <p>{% set anneeLg = lastlogin|date("Y") %}</p>
                    <p>{% set anneeNow = "now"|date("Y") %} </p>
                    <p>{% set moisLg = lastlogin|date("m") %}</p>
                    <p>{% set moisNow = "now"|date("m") %} </p>
                    <p>{% set jourLg = lastlogin|date("d") %}</p>
                    <p>{% set jourNow = "now"|date("d") %} </p>
                    <p>{% set heureLg = lastlogin|date("H") %}</p>
                    <p>{% set heureNow = "now"|date("H") %} </p>
                    <p>{% set minuteLg = lastlogin|date("i") %}</p>
                    <p>{% set minuteNow = "now"|date("i") %} </p>

                    {% set anneeSous = anneeNow - anneeLg %}
                    {% set anneeSousMinute = anneeSous * 12 * 30 * 24 * 60 %}
                    #}{#<p>{{ anneeSous }}</p>#}{#
                    {% set moisSous = moisNow - moisLg %}
                    {% set moisSousMinute = moisSous * 30 * 24 * 60 %}
                    #}{#<p>{{ moisSous }}</p>#}{#
                    {% set jourSous = jourNow - jourLg %}
                    {% set jourSousMinute = jourSous * 24 * 60 %}
                    #}{#<p>{{ jourSous }}</p>#}{#
                    {% set heureSous = heureNow - heureLg %}
                    {% set heureSousMinute = heureSous * 60 %}
                    #}{#<p>{{ heureSous }}</p>#}{#
                    {% set minuteSous = minuteNow - minuteLg %}
                    {% set minuteSousMinute = minuteSous %}
                    #}{#<p>{{ minuteSous }}</p>#}{#

                    {% if "now"|date("YmdHi") != lastlogin|date("YmdHi") %}
                    <p>Cela fait
                        {% if anneeSous == 1 or anneeSous == -1 %}
                            {{ anneeSous|replace({'-': ''}) }} an
                        {% endif %}
                        {% if anneeSous != 0 and anneeSous != 1 %}
                            {{ anneeSous|replace({'-': ''}) }} ans
                        {% endif %}

                        {% if moisSous != 0 %}
                            {{ moisSous|replace({'-': ''}) }} mois
                        {% endif %}

                        {% if jourSous == 1 or jourSous == -1 %}
                            {{ jourSous|replace({'-': ''}) }} jour
                        {% endif %}
                        {% if jourSous != 0 and jourSous != 1 %}
                            {{ jourSous|replace({'-': ''}) }} jours
                        {% endif %}

                        {% if heureSous == 1 or heureSous == -1 %}
                            {{ heureSous|replace({'-': ''}) }} heure
                        {% endif %}
                        {% if heureSous != 0 and heureSous != 1 %}
                            {{ heureSous|replace({'-': ''}) }} heures
                        {% endif %}

                        {% if minuteSous == 1 or minuteSous == -1 %}
                            {{ minuteSous|replace({'-': ''}) }} minute
                        {% endif %}
                        {% if minuteSous != 0 and minuteSous != 1 %}
                            {{ minuteSous|replace({'-': ''}) }} minutes
                        {% endif %}
                        que vous avez été absent.
                    </p>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>#}
    {#{% endif %}#}
    {#<div class="container">#}
    <div style="margin-left:260px;">
        <div>
            <div class="col-12 {#d-flex justify-content-center#} mt-3 mb-0">
                <ul class="nav nav-tabs" id="myTab" role="tablist">
                    <li class="nav-item">
                        <a class="nav-link active" id="list-tab" data-toggle="tab" href="#list" role="tab" aria-controls="list" aria-selected="true">Liste des chambres</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="add-tab" data-toggle="tab" href="#add" role="tab" aria-controls="add" aria-selected="false">Ajouter une chambre</a>
                    </li>
                </ul>
                <div class="tab-content" id="myTabContent">
                    <div class="tab-pane fade show active" id="list" role="tabpanel" aria-labelledby="list-tab">
                        <table class="table mt-1">
                            <thead>
                            <tr class="bg-header-table">
                                <th scope="col">id</th>
                                <th scope="col">Nom</th>
                                <th scope="col">Nb M²</th>
                                <th scope="col">Nb pièce</th>
                                <th scope="col">Nom de l'hôtel</th>
                                <th scope="col">Action</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for chambre in chambres %}
                                <tr>
                                    <th class="tabOutil">{{ chambre.id }}</th>
                                    <td class="tabOutil">{{ chambre.nom }}</td>
                                    <td class="tabOutil">{{ chambre.nbmcarre }}</td>
                                    <td class="tabOutil">{{ chambre.nbpiece }}</td>

                                    <td class="tabOutil">{{ chambre.hotel.nom }}</td>
                                    <td>
                                        <a class="tabOutil btn btn-primary" href="{{ path('app_chambre_show', {'id': chambre.id }) }}"><i class="fa fa-search-plus" aria-hidden="true"></i></a>
                                        <a class="tabOutil btn btn-danger" href="{{ path('app_chambre_delete', {'id': chambre.id }) }}"><i class="fa fa-trash" aria-hidden="true"></i></a>
                                        <a class="tabOutil btn btn-success" href="{{ path('app_outil_update', {'id': chambre.id }) }}"><i class="fa fa-pencil" aria-hidden="true"></i></a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    <div class="tab-pane fade mt-1" id="add" role="tabpanel" aria-labelledby="add-tab">
                        <div class="row">
                            <div class="col-2"></div>
                            <div class="col-8">
                                {{ form_start(form) }}
                                {{ form_widget(form) }}
                                <button class="mt-3 btn btn-outline-primary" type="submit">Ajouter</button>
                                {{ form_end(form) }}
                            </div>
                            <div class="col-2"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}