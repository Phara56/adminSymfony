{% extends 'base.html.twig' %}

{% block body %}
    {% if role == "ROLE_SUPER_ADMIN" %}
        <div class="text-center m-2 btnAddH">
            <a href="#" class="btn btn-primary">Ajouter un hôtelier</a>
        </div>
    {% endif %}

    {#<div class="container">#}
    <div style="padding-left:260px;">

    <h2 class="text-center">Modification Ville</h2>
    {#<div class="row">
    <div class="col-2"></div>#}
    <div class="col-12">
        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <button class="mt-3 btn btn-outline-primary" type="submit">Modifier</button>
        {{ form_end(edit_form) }}
        {#</div>
            <div class="col-2"></div>
        </div>#}
    </div>

{% endblock %}